div.settings-list-item-holder
    a.settings-list-item.editable(href="profile")
        div.settings-list-item-container
            div.settings-list-content
                div.setting-name
                    if (user.status === 'new' || user.status === 'disapproved')
                        div.step-sign(class=(profile ? 'ready': ''))
                            if (profile)
                                i.material-icons.done-icon done
                            else
                                | 1
                    b Профиль
                div.setting-value
                    | #{profile ? profile.name : 'надо выбрать'}
                    if (user.needBel)
                        p(style="color: #888; margin: 0; line-height: 1.2; font-size: 14px;") Вы выбрали белорусский язык заданий

            div.settings-list-next-sign
                i.material-icons keyboard_arrow_right